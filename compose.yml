services:
  postgres:
    image: 'postgres:latest'
    environment:
      - POSTGRES_DB=athenhub
      - POSTGRES_USER=athenhub
      - POSTGRES_PASSWORD=@@Athenhub01
    ports:
      - '5432:5432'
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql